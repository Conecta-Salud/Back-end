# configure your datasource
quarkus.datasource.db-kind = mysql
quarkus.datasource.username = ${DB_USERNAME}
quarkus.datasource.password = ${DB_PASSWORD}
quarkus.datasource.jdbc.url = ${DB_URL}

quarkus.http.cors.enabled=true
quarkus.http.cors.origins=${CORS_ORIGINS}
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=authorization,content-type
quarkus.http.cors.access-control-allow-credentials=true

# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.schema-management.strategy=validate

quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=0
# If the local schema was created manually and no Flyway migrations exist yet,
# temporarily use quarkus.flyway.migrate-at-start=false for local startup.

# Firebase Admin SDK.
# Required binding: firebase.service-account-location=${FIREBASE_SERVICE_ACCOUNT}
# Keep the service account JSON outside the repository and provide its filesystem path through FIREBASE_SERVICE_ACCOUNT.
firebase.service-account-location=${FIREBASE_SERVICE_ACCOUNT:}

# L�mite interno de ConectaSalud para archivos CSV
conectasalud.upload.storage-dir=uploads
conectasalud.upload.max-file-size-bytes=104857600

# L�mite HTTP para requests multipart grandes
quarkus.http.limits.max-body-size=100M
# OpenAPI / Swagger UI
quarkus.smallrye-openapi.path=/openapi
quarkus.smallrye-openapi.info.title=ConectaSalud API
quarkus.smallrye-openapi.info.version=1.0.0
quarkus.smallrye-openapi.info.description=Documentación de la API de ConectaSalud
quarkus.swagger-ui.path=/swagger-ui
quarkus.swagger-ui.always-include=true
# Test profile: H2 in-memory database
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.username=sa
%test.quarkus.datasource.password=
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MYSQL
%test.quarkus.hibernate-orm.schema-management.strategy=none
%test.quarkus.hibernate-orm.sql-load-script=no-file
%test.quarkus.flyway.migrate-at-start=false
%test.firebase.service-account-location=none
