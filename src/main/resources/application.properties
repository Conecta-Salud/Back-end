# configure your datasource
quarkus.datasource.db-kind = mysql
quarkus.datasource.username = root
quarkus.datasource.password = DBlaptop
quarkus.datasource.jdbc.url = jdbc:mysql://localhost:3306/todogrupo1

# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.schema-management.strategy = update

# Firebase admin SDK - path to service account JSON (mount as secret / volume)
firebase.service-account-location=src/main/resources/todolistgrupo1-firebase-adminsdk-fbsvc-a820491f78.json

# Test profile: H2 in-memory database
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.username=sa
%test.quarkus.datasource.password=
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
%test.quarkus.hibernate-orm.schema-management.strategy=drop-and-create
%test.quarkus.hibernate-orm.sql-load-script=no-file
%test.firebase.service-account-location=none